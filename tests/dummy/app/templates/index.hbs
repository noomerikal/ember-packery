<a href="#" {{action 'switchObjects'}}>Switch Models</a>

{{#packery items=currentObject.items}}
  {{#each currentObject.items}}
    <div class="item">
      <img src="{{imgsrc}}"><br>
      {{name}}<br>
      {{input type="text" value=name}}
    </div>
  {{/each}}
{{/packery}}
